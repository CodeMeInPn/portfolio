---
import Section from '@components/Section.astro';
---

<Section
  activeBars={{
    position: ['horizontal', 'right'],
    options: { offset: ['top', 'left'], invertColor: ['right'] },
  }}
  activeConnectors={{
    position: ['bottom-left-inverted', 'top-right'],
    offset: ['top-right'],
  }}
>
  <slot />
</Section>
